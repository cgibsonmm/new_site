%p.text-muted
  Post by:
  = forum_post.user.username
%p
  = forum_post.body


- unless @forum_thread.forum_posts.first == forum_post
  = link_to 'X', [@forum_thread, forum_post], method: :delete, data: {confirm: "Are you sure?"}
%hr
